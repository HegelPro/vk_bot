export const messages = {
  send: 'messages.send',
  getLongPollServer: 'messages.getLongPollServer'
}

export const photos = {
  getMessagesUploadServer: 'photos.getMessagesUploadServer',
  saveMessagesPhoto: 'photos.saveMessagesPhoto',
}

export const docs = {
  getMessagesUploadServer: 'docs.getMessagesUploadServer',
  save: 'docs.save',
}

export default {
  messages,
  photos,
  docs
}